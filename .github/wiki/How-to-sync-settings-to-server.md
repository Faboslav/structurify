After configuring the settings, a `structurify.json` file will be generated in the `config` directory. This file contains all configured settings.

To ensure the same behavior on a server, the `structurify.json` file must also be copied to the server’s `config` directory.